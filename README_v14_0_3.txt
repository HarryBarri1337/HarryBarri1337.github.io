SkinQuest v14.0.3

What to do:
1. Copy/replace the site files with this version.
2. If your existing project is already on v14.0.2, run NO SQL for this release.
3. For a brand-new empty Supabase project, use:
   skinquest_full_setup_v14_0_3.sql

v14.0.3 PATCH
- Cleaned up the notifications drawer and replaced the browser-default scrollbar styling
- Reward notifications now open and highlight the exact dashboard request
- Dashboard data loads in parallel and tab returns refresh silently without hiding the page
- Removed BitLabs and Lootably from Surveys
- Added the official CPX Research logo to the survey card
- No database changes
