SkinQuest v13.1.0 release checks
Date: 2026-08-11

Passed locally
- app.js and sw.js JavaScript syntax checks.
- All 10 included Edge Functions bundled successfully with external imports preserved.
- The BitLabs HMAC-SHA1 implementation matched BitLabs' published test vector.
- All 13 HTML documents parsed successfully.
- Every local HTML href/src resolves to an included file or a known clean route.
- manifest.json parses as valid JSON.
- CSS braces and SQL dollar-quoted bodies passed structural sanity checks.
- Refund XP exclusion exists in both the frontend calculation and both v13.1 SQL paths.
- BitLabs secrets are referenced only through Edge Function environment variables; no credential values are included.
- v13.1 version, asset-query, and service-worker cache identifiers are synchronized.

Deployment-dependent checks still required
- Run skinquest_upgrade_existing_to_v13_1_0.sql on the backed-up current Supabase project.
- Add BITLABS_S2S_TOKEN and deploy the three functions listed in README_v13_1_0.txt.
- Configure and test the BitLabs reward callback in the publisher dashboard.
- Confirm the BitLabs currency factor matches the intended SkinQuest coin economy.
- Verify the final mobile appearance on physical iOS and Android devices.
- Test a real BitLabs completion, duplicate callback, reconciliation, refund, and account deletion.

Validation note
The environment had no browser executable, so pixel-level mobile screenshots and live Supabase/BitLabs calls could not be run locally. The responsive CSS, markup, references, JavaScript, Edge bundles, HMAC vector, and SQL structure were checked statically.
